# <%= @cookbook_name %>

This cookbook will take HP Sum action for the following base line:
  <%= @remote_location %>

It contains the following recipes:
  inventory_check
  parse_node_data
  generate_audit_events
  package_update
